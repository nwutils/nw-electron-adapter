const helpers = {
  notPlanned: function () {
    console.log('___________________');
    console.log('nw-electron-adapter');
    console.log('This API mapping has not been planned yet.');
    console.log('Search for this API item in the issues and +1 it.');
    console.log('* https://github.com/nwutils/nw-electron-adapter/issues');
    console.log('Create an issue for it with the API name as the issue title if one does not exist.');
  }
};

module.exports = helpers;
